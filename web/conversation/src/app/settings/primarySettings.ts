export const PRIMARY_SETTINGS_UNAVAILABLE_MESSAGE =
  "This setting is stored on the hub, and this session is not connected to one.";

export function usePrimarySettingsAvailable(): boolean {
  return true;
}
