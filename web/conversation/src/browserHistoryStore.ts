/** Where a thread's preview went. Nothing stores it here. */
export function recordVisitForThread(_threadRef: unknown, _url: string): void {
  // Intentionally empty: no preview history in a browser client.
}
